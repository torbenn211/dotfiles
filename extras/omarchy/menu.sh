#!/usr/bin/env bash
# Legacy placeholder kept so old packages can uninstall cleanly.
#
# The theme no longer installs an Omarchy menu override. Omarchy's upstream
# menu changes frequently, so the installer restores/removes the old override
# and leaves ~/.config/omarchy/extensions/menu.sh alone afterward.
